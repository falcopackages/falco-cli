# IMPORTS:START
from django.forms import ModelForm

from .models import {{model_name}}

# IMPORTS:END


# CODE:START
class {{model_name}}Form(ModelForm):
    class Meta:
        model = {{model_name}}
        fields = (
        {% for item in model_fields if item not in excluded_fields %}
            {% if not loop.first %}, {% endif %}"{{ item }}"
        {% endfor %}
        )


# CODE:END
